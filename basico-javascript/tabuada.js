function mostra(frase) {
  document.write(`${frase} <br>`);
};

// var multiplicador = 1;
// while (multiplicador <= 10) {
//   mostra(`O resultado de 7 x ${multiplicador} é igual a ${7 * multiplicador}`);
//   multiplicador++;
// }

for (multiplicador = 1; multiplicador <= 10; multiplicador++) {
  mostra(`O resultado de 7 x ${multiplicador} é igual a ${7 * multiplicador}`);
}
